name: Build NLP Actors

on:
  push:
    branches: [ main, master ]
    paths:
      - 'images/nlp/**'
      - 'entrypoints/**'
      - '.github/workflows/build-nlp.yml'
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up QEMU
        uses: docker/setup-qemu-action@v3

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push
        uses: docker/build-push-action@v5
        with:
          context: ./images/nlp
          push: true
          tags: ghcr.io/${{ github.repository_owner }}/stories-nlp-actor:latest
          cache-from: type=registry,ref=ghcr.io/${{ github.repository_owner }}/stories-nlp-actor:cache
          cache-to: type=registry,ref=ghcr.io/${{ github.repository_owner }}/stories-nlp-actor:cache,mode=max
